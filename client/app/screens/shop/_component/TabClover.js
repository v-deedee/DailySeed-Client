import { View, Text, StyleSheet } from "react-native";

export default function TabClover() {
    return (
        <View style={styles.container}>
            <Text>TabClover</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        padding: 3
    },
});
